1.sourcefile_rawtext.txt = ไฟล์ต้นทางที่มี Text ก่อนการทำ sanitize ครับ
2.targetfile_sanitizedtext.txt = ไฟล์ target ที่มี Text หลังการทำ sanitize ครับ
3.textsanitizer.py และ textsanitizer_concept_oop.py = เป็น Application ที่เอาไว้ใช้ทำ Text Sanitization ครับโดยไฟล์ textsanitizer_concept_oop.py จะให้ผลลัพธ์เหมือนกับไฟล์
textsanitizer.py ครับแต่ว่าผมได้เขียนเป็น Class ตาม OOP Concept และยังเพิ่มในส่วนของ Extension Code ไว้ตาม Assignment ครับ
4.File .ipynb เป็น IDE จาก Jupyter Notebook ที่ใช้พัฒนาโปรแกรมในข้อ 3 ครับ

หมายเหตุ : ที่โปรแกรมข้อ 3 จะขออนุญาต Update เพิ่มเติมอีกครั้งภายใน 5 โมงเย็นของวันนี้ครับ โดยผมอยากจะทำให้เป็น Class
ตาม OOP Concept เพิ่มเข้าไปครับ
